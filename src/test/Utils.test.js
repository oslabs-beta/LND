import { Utils } from '../app/Utils';
describe('Utils test suits', function () {
    test('first test', function () {
        var abc = Utils.toUpperCase('abc');
        console.log('It works!');
    });
});
